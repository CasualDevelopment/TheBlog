<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8"/>
        <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
        <meta
      name="description"
      content="The Blog, built as an example template for the YouTube Channel @casualdevelopment. This site uses Eleventy with WebC and Netlify CMS to serve it's content."
/>
        <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
        <script src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>
        <link rel="stylesheet" href="styles/styles.css"/>
        <title>The Blog</title>
    </head>
    <body>
        {%include "header.njk"%}
        <main>
            {{ content | safe }}
        </main>
        {# maybe include footer #}
        <script>
            if (window.netlifyIdentity) {
                window
                    .netlifyIdentity
                    .on("init", user => {
                        if (!user) {
                            window
                                .netlifyIdentity
                                .on("login", () => {
                                    document.location.href = "/admin/";
                                });
                        }
                    });
            }
        </script>
    </body>
</html>